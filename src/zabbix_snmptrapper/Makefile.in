# Build rules

all:

	@CC@ -o ../../bin/zabbix_snmptrapper @CFLAGS@ -I../../include ../../include/cfg.c ../../include/log.c zabbix_snmptrapper.c @LIBS@

clean:
	rm -f *.o 
	rm -f ../../bin/zabbix_snmptrapper

