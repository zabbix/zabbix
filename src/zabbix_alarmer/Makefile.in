# Build rules

all:

	@CC@ @LIBS@ @CFLAGS@ -ggdb -I /usr/include/mysql -I../../include  -L/usr/lib/mysql zabbix_alarmer.c ../../include/expression.c ../../include/db.c -o zabbix_alarmer -Wall
	strip zabbix_alarmer

clean:
	rm -fv *.o 

