# Build rules

INCLUDE = @MYSQL_INCLUDE@ @PGSQL_INCLUDE@
LIBS    = @LIBS@ @MYSQL_LFLAGS@ @PGSQL_LFLAGS@

all:

	@CC@ -o zabbix_sucker @CFLAGS@ -I../../include $(INCLUDE) zabbix_sucker.c ../../include/db.c ../../include/functions.c ../../include/expression.c -Wall $(LIBS) 

clean:
	rm -fv *.o 

