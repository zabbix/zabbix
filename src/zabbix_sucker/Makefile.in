# Build rules

all:

	@CC@ -o zabbix_sucker @CFLAGS@ -ggdb -I /usr/include/mysql -I../../include  -L/usr/lib/mysql zabbix_sucker.c ../../include/db.c ../../include/functions.c -Wall @LIBS@
	strip zabbix_sucker

clean:
	rm -fv *.o 

