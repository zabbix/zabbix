# Build rules

all:

	@CC@ -o ../../bin/zabbix_agent  @CFLAGS@ -I../../include zabbix_agent.c sysinfo.c ../../include/log.c ../../include/cfg.c ../../include/security.c -Wall @LIBS@
	@CC@ -o ../../bin/zabbix_agentd @CFLAGS@ -I../../include zabbix_agentd.c sysinfo.c stats.c interfaces.c diskdevices.c ../../include/log.c ../../include/cfg.c ../../include/security.c -Wall @LIBS@

clean:
	rm -fv *.o 
	rm -fv ../../bin/zabbix_agent
	rm -fv ../../bin/zabbix_agentd

