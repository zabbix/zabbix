/**
 * Autogenerated by Thrift Compiler (0.7.0)
 *
 * DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
 */
/**
 * A special note for Zabbix users:
 *    This file has been generated from file
 *    apache-cassandra-1.0.7-src/interface/cassandra.thrift
 *    by an IN-HOUSE PATCHED Thrift Compiler 0.7.0.
 *    Thrift was patched as suggested in a comment by Jerry Gally, 16/Oct/11 20:20
 *    at https://issues.apache.org/jira/browse/THRIFT-1389
 *    to prevent a large memory leak:
 * ------------------------------------------------
 * The following change to t_c_glib_generator.cc appears to fix the leak:
 *
 *1733c1733
 *< indent(f_types_impl_) << "object->" << name << " = g_object_new (" << this->nspace_uc << "TYPE_" << type_name_uc << ", NULL);" << endl;
 *--
 *> indent(f_types_impl_) << "object->" << name << " = NULL;" << endl;
 *------------------------------------------------
 *
 * After this file was generated by Thrift in some places 'value' has been manually renamed to 'value2'
 * to avoid conflict with Zabbix variable name.
 */
#include <string.h>
#include <thrift.h>
#include <thrift_application_exception.h>
#include "cassandra.h"

gboolean
cassandra_if_login (CassandraIf *iface, const AuthenticationRequest * auth_request, AuthenticationException ** authnx, AuthorizationException ** authzx, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->login (iface, auth_request, authnx, authzx, error);
}

gboolean
cassandra_if_set_keyspace (CassandraIf *iface, const gchar * keyspace, InvalidRequestException ** ire, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->set_keyspace (iface, keyspace, ire, error);
}

gboolean
cassandra_if_get (CassandraIf *iface, ColumnOrSuperColumn ** _return, const GByteArray * key, const ColumnPath * column_path, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, NotFoundException ** nfe, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->get (iface, _return, key, column_path, consistency_level, ire, nfe, ue, te, error);
}

gboolean
cassandra_if_get_slice (CassandraIf *iface, GPtrArray ** _return, const GByteArray * key, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->get_slice (iface, _return, key, column_parent, predicate, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_get_count (CassandraIf *iface, gint32* _return, const GByteArray * key, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->get_count (iface, _return, key, column_parent, predicate, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_multiget_slice (CassandraIf *iface, GHashTable ** _return, const GPtrArray * keys, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->multiget_slice (iface, _return, keys, column_parent, predicate, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_multiget_count (CassandraIf *iface, GHashTable ** _return, const GPtrArray * keys, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->multiget_count (iface, _return, keys, column_parent, predicate, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_get_range_slices (CassandraIf *iface, GPtrArray ** _return, const ColumnParent * column_parent, const SlicePredicate * predicate, const KeyRange * range, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->get_range_slices (iface, _return, column_parent, predicate, range, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_get_indexed_slices (CassandraIf *iface, GPtrArray ** _return, const ColumnParent * column_parent, const IndexClause * index_clause, const SlicePredicate * column_predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->get_indexed_slices (iface, _return, column_parent, index_clause, column_predicate, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_insert (CassandraIf *iface, const GByteArray * key, const ColumnParent * column_parent, const Column * column, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->insert (iface, key, column_parent, column, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_add (CassandraIf *iface, const GByteArray * key, const ColumnParent * column_parent, const CounterColumn * column, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->add (iface, key, column_parent, column, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_remove (CassandraIf *iface, const GByteArray * key, const ColumnPath * column_path, const gint64 timestamp, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->remove (iface, key, column_path, timestamp, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_remove_counter (CassandraIf *iface, const GByteArray * key, const ColumnPath * path, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->remove_counter (iface, key, path, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_batch_mutate (CassandraIf *iface, const GHashTable * mutation_map, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->batch_mutate (iface, mutation_map, consistency_level, ire, ue, te, error);
}

gboolean
cassandra_if_truncate (CassandraIf *iface, const gchar * cfname, InvalidRequestException ** ire, UnavailableException ** ue, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->truncate (iface, cfname, ire, ue, error);
}

gboolean
cassandra_if_describe_schema_versions (CassandraIf *iface, GHashTable ** _return, InvalidRequestException ** ire, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->describe_schema_versions (iface, _return, ire, error);
}

gboolean
cassandra_if_describe_keyspaces (CassandraIf *iface, GPtrArray ** _return, InvalidRequestException ** ire, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->describe_keyspaces (iface, _return, ire, error);
}

gboolean
cassandra_if_describe_cluster_name (CassandraIf *iface, gchar ** _return, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->describe_cluster_name (iface, _return, error);
}

gboolean
cassandra_if_describe_version (CassandraIf *iface, gchar ** _return, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->describe_version (iface, _return, error);
}

gboolean
cassandra_if_describe_ring (CassandraIf *iface, GPtrArray ** _return, const gchar * keyspace, InvalidRequestException ** ire, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->describe_ring (iface, _return, keyspace, ire, error);
}

gboolean
cassandra_if_describe_partitioner (CassandraIf *iface, gchar ** _return, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->describe_partitioner (iface, _return, error);
}

gboolean
cassandra_if_describe_snitch (CassandraIf *iface, gchar ** _return, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->describe_snitch (iface, _return, error);
}

gboolean
cassandra_if_describe_keyspace (CassandraIf *iface, KsDef ** _return, const gchar * keyspace, NotFoundException ** nfe, InvalidRequestException ** ire, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->describe_keyspace (iface, _return, keyspace, nfe, ire, error);
}

gboolean
cassandra_if_describe_splits (CassandraIf *iface, GPtrArray ** _return, const gchar * cfName, const gchar * start_token, const gchar * end_token, const gint32 keys_per_split, InvalidRequestException ** ire, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->describe_splits (iface, _return, cfName, start_token, end_token, keys_per_split, ire, error);
}

gboolean
cassandra_if_system_add_column_family (CassandraIf *iface, gchar ** _return, const CfDef * cf_def, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->system_add_column_family (iface, _return, cf_def, ire, sde, error);
}

gboolean
cassandra_if_system_drop_column_family (CassandraIf *iface, gchar ** _return, const gchar * column_family, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->system_drop_column_family (iface, _return, column_family, ire, sde, error);
}

gboolean
cassandra_if_system_add_keyspace (CassandraIf *iface, gchar ** _return, const KsDef * ks_def, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->system_add_keyspace (iface, _return, ks_def, ire, sde, error);
}

gboolean
cassandra_if_system_drop_keyspace (CassandraIf *iface, gchar ** _return, const gchar * keyspace, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->system_drop_keyspace (iface, _return, keyspace, ire, sde, error);
}

gboolean
cassandra_if_system_update_keyspace (CassandraIf *iface, gchar ** _return, const KsDef * ks_def, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->system_update_keyspace (iface, _return, ks_def, ire, sde, error);
}

gboolean
cassandra_if_system_update_column_family (CassandraIf *iface, gchar ** _return, const CfDef * cf_def, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->system_update_column_family (iface, _return, cf_def, ire, sde, error);
}

gboolean
cassandra_if_execute_cql_query (CassandraIf *iface, CqlResult ** _return, const GByteArray * query, const Compression compression, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, SchemaDisagreementException ** sde, GError **error)
{
  return CASSANDRA_IF_GET_INTERFACE (iface)->execute_cql_query (iface, _return, query, compression, ire, ue, te, sde, error);
}

GType
cassandra_if_get_type (void)
{
  static GType type = 0;
  if (type == 0)
  {
    static const GTypeInfo type_info =
    {
      sizeof (CassandraIfInterface),
      NULL,  /* base_init */
      NULL  /* base_finalize */
    };
    type = g_type_register_static (G_TYPE_INTERFACE,
                                   "CassandraIf",
                                   &type_info, 0);
  }
  return type;
}

static void 
cassandra_if_interface_init (CassandraIfInterface *iface);

G_DEFINE_TYPE_WITH_CODE (CassandraClient, cassandra_client,
                       G_TYPE_OBJECT, 
                         G_IMPLEMENT_INTERFACE (TYPE_CASSANDRA_IF,
                                                cassandra_if_interface_init));

enum _CassandraClientProperties
{
  PROP_0,
  PROP_CASSANDRA_CLIENT_INPUT_PROTOCOL,
  PROP_CASSANDRA_CLIENT_OUTPUT_PROTOCOL
};

void
cassandra_client_set_property (GObject *object, guint property_id, const GValue *value, GParamSpec *pspec)
{
  CassandraClient *client = CASSANDRA_CLIENT (object);

  THRIFT_UNUSED_VAR (pspec);

  switch (property_id)
  {
    case PROP_CASSANDRA_CLIENT_INPUT_PROTOCOL:
      client->input_protocol = g_value_get_object (value);
      break;
    case PROP_CASSANDRA_CLIENT_OUTPUT_PROTOCOL:
      client->output_protocol = g_value_get_object (value);
      break;
  }
}

void
cassandra_client_get_property (GObject *object, guint property_id, GValue *value, GParamSpec *pspec)
{
  CassandraClient *client = CASSANDRA_CLIENT (object);

  THRIFT_UNUSED_VAR (pspec);

  switch (property_id)
  {
    case PROP_CASSANDRA_CLIENT_INPUT_PROTOCOL:
      g_value_set_object (value, client->input_protocol);
      break;
    case PROP_CASSANDRA_CLIENT_OUTPUT_PROTOCOL:
      g_value_set_object (value, client->output_protocol);
      break;
  }
}

gboolean cassandra_client_send_login (CassandraIf * iface, const AuthenticationRequest * auth_request, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "login", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "login_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "auth_request", T_STRUCT, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (auth_request), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_login (CassandraIf * iface, AuthenticationException ** authnx, AuthorizationException ** authzx, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "login", 5) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected login", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            *authnx = g_object_new (TYPE_AUTHENTICATION_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*authnx), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *authzx = g_object_new (TYPE_AUTHORIZATION_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*authzx), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*authnx != NULL)
  {
      g_set_error (error, AUTHENTICATION_EXCEPTION_ERROR, AUTHENTICATION_EXCEPTION_ERROR_CODE, "AuthenticationException");
      return FALSE;
  }
  if (*authzx != NULL)
  {
      g_set_error (error, AUTHORIZATION_EXCEPTION_ERROR, AUTHORIZATION_EXCEPTION_ERROR_CODE, "AuthorizationException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_login (CassandraIf * iface, const AuthenticationRequest * auth_request, AuthenticationException ** authnx, AuthorizationException ** authzx, GError ** error)
{
  if (!cassandra_client_send_login (iface, auth_request, error))
    return FALSE;
  if (!cassandra_client_recv_login (iface, authnx, authzx, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_set_keyspace (CassandraIf * iface, const gchar * keyspace, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "set_keyspace", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "set_keyspace_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "keyspace", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_string (protocol, keyspace, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_set_keyspace (CassandraIf * iface, InvalidRequestException ** ire, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "set_keyspace", 12) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected set_keyspace", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_set_keyspace (CassandraIf * iface, const gchar * keyspace, InvalidRequestException ** ire, GError ** error)
{
  if (!cassandra_client_send_set_keyspace (iface, keyspace, error))
    return FALSE;
  if (!cassandra_client_recv_set_keyspace (iface, ire, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_get (CassandraIf * iface, const GByteArray * key, const ColumnPath * column_path, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "get", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "get_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "key", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) key)->data, ((GByteArray *) key)->len, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_path", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_path), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_get (CassandraIf * iface, ColumnOrSuperColumn ** _return, InvalidRequestException ** ire, NotFoundException ** nfe, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "get", 3) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected get", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRUCT)
          {
            *_return = g_object_new (TYPE_COLUMN_OR_SUPER_COLUMN, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*_return), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *nfe = g_object_new (TYPE_NOT_FOUND_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*nfe), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 4:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*nfe != NULL)
  {
      g_set_error (error, NOT_FOUND_EXCEPTION_ERROR, NOT_FOUND_EXCEPTION_ERROR_CODE, "NotFoundException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_get (CassandraIf * iface, ColumnOrSuperColumn ** _return, const GByteArray * key, const ColumnPath * column_path, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, NotFoundException ** nfe, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_get (iface, key, column_path, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_get (iface, _return, ire, nfe, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_get_slice (CassandraIf * iface, const GByteArray * key, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "get_slice", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "get_slice_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "key", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) key)->data, ((GByteArray *) key)->len, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_parent", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_parent), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "predicate", T_STRUCT, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (predicate), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_get_slice (CassandraIf * iface, GPtrArray ** _return, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "get_slice", 9) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected get_slice", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              ThriftType element_type;
              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type, &size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              guint32 i;
              for (i = 0; i < size; i++)
              {
                ColumnOrSuperColumn * _elem29;
                _elem29 = g_object_new (TYPE_COLUMN_OR_SUPER_COLUMN, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem29), protocol, error)) < 0)
                  return 0;
                xfer += ret;
                g_ptr_array_add (*_return, _elem29);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;

            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_get_slice (CassandraIf * iface, GPtrArray ** _return, const GByteArray * key, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_get_slice (iface, key, column_parent, predicate, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_get_slice (iface, _return, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_get_count (CassandraIf * iface, const GByteArray * key, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "get_count", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "get_count_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "key", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) key)->data, ((GByteArray *) key)->len, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_parent", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_parent), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "predicate", T_STRUCT, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (predicate), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_get_count (CassandraIf * iface, gint32* _return, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "get_count", 9) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected get_count", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_I32)
          {
            if ((ret = thrift_protocol_read_i32 (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_get_count (CassandraIf * iface, gint32* _return, const GByteArray * key, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_get_count (iface, key, column_parent, predicate, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_get_count (iface, _return, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_multiget_slice (CassandraIf * iface, const GPtrArray * keys, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "multiget_slice", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "multiget_slice_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "keys", T_LIST, 1, error)) < 0)
      return 0;
    xfer += ret;
    {
      if ((ret = thrift_protocol_write_list_begin (protocol, T_STRING, (gint32) keys->len, error)) < 0)
        return 0;
      xfer += ret;
      guint i;
      for (i=0; i<keys->len; i++)
      {
        if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) g_ptr_array_index ((GPtrArray *) keys, i))->data, ((GByteArray *) g_ptr_array_index ((GPtrArray *) keys, i))->len, error)) < 0)
          return 0;
      }
      if ((ret = thrift_protocol_write_list_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_parent", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_parent), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "predicate", T_STRUCT, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (predicate), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_multiget_slice (CassandraIf * iface, GHashTable ** _return, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "multiget_slice", 14) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected multiget_slice", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_MAP)
          {
            {
              guint32 size;
              ThriftType key_type;
              ThriftType value_type;

              /* read the map begin marker */
              if ((ret = thrift_protocol_read_map_begin (protocol, &key_type, &value_type, &size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through each of the map's fields */
              guint32 i;
              for (i = 0; i < size; i++)
              {
                GByteArray * key30;
                GPtrArray * val31;
                if ((ret = thrift_protocol_read_binary (protocol, &data, &len, error)) < 0)
                  return 0;
                xfer += ret;
                key30 = g_byte_array_new();
                g_byte_array_append (key30, (guint8 *) data, (guint) len);
                g_free (data);
                {
                  guint32 size;
                  ThriftType element_type;
                  if ((ret = thrift_protocol_read_list_begin (protocol, &element_type, &size, error)) < 0)
                    return 0;
                  xfer += ret;

                  /* iterate through list elements */
                  guint32 i;
                  for (i = 0; i < size; i++)
                  {
                    ColumnOrSuperColumn * _elem32;
                    _elem32 = g_object_new (TYPE_COLUMN_OR_SUPER_COLUMN, NULL);
                    if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem32), protocol, error)) < 0)
                      return 0;
                    xfer += ret;
                    g_ptr_array_add (val31, _elem32);
                  }
                  if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                    return 0;
                  xfer += ret;

                }
                g_hash_table_insert ((GHashTable *)*_return, (gpointer) &key30, (gpointer) &val31);
              }

              /* read the map end marker */
              if ((ret = thrift_protocol_read_map_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_multiget_slice (CassandraIf * iface, GHashTable ** _return, const GPtrArray * keys, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_multiget_slice (iface, keys, column_parent, predicate, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_multiget_slice (iface, _return, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_multiget_count (CassandraIf * iface, const GPtrArray * keys, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "multiget_count", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "multiget_count_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "keys", T_LIST, 1, error)) < 0)
      return 0;
    xfer += ret;
    {
      if ((ret = thrift_protocol_write_list_begin (protocol, T_STRING, (gint32) keys->len, error)) < 0)
        return 0;
      xfer += ret;
      guint i;
      for (i=0; i<keys->len; i++)
      {
        if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) g_ptr_array_index ((GPtrArray *) keys, i))->data, ((GByteArray *) g_ptr_array_index ((GPtrArray *) keys, i))->len, error)) < 0)
          return 0;
      }
      if ((ret = thrift_protocol_write_list_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_parent", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_parent), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "predicate", T_STRUCT, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (predicate), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_multiget_count (CassandraIf * iface, GHashTable ** _return, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "multiget_count", 14) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected multiget_count", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_MAP)
          {
            {
              guint32 size;
              ThriftType key_type;
              ThriftType value_type;

              /* read the map begin marker */
              if ((ret = thrift_protocol_read_map_begin (protocol, &key_type, &value_type, &size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through each of the map's fields */
              guint32 i;
              for (i = 0; i < size; i++)
              {
                GByteArray * key33;
                gint32* val34 = g_new (gint32, 1);
                if ((ret = thrift_protocol_read_binary (protocol, &data, &len, error)) < 0)
                  return 0;
                xfer += ret;
                key33 = g_byte_array_new();
                g_byte_array_append (key33, (guint8 *) data, (guint) len);
                g_free (data);
                if ((ret = thrift_protocol_read_i32 (protocol, &*val34, error)) < 0)
                  return 0;
                xfer += ret;
                g_hash_table_insert ((GHashTable *)*_return, (gpointer) &key33, (gpointer) val34);
              }

              /* read the map end marker */
              if ((ret = thrift_protocol_read_map_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_multiget_count (CassandraIf * iface, GHashTable ** _return, const GPtrArray * keys, const ColumnParent * column_parent, const SlicePredicate * predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_multiget_count (iface, keys, column_parent, predicate, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_multiget_count (iface, _return, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_get_range_slices (CassandraIf * iface, const ColumnParent * column_parent, const SlicePredicate * predicate, const KeyRange * range, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "get_range_slices", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "get_range_slices_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_parent", T_STRUCT, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_parent), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "predicate", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (predicate), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "range", T_STRUCT, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (range), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_get_range_slices (CassandraIf * iface, GPtrArray ** _return, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "get_range_slices", 16) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected get_range_slices", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              ThriftType element_type;
              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type, &size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              guint32 i;
              for (i = 0; i < size; i++)
              {
                KeySlice * _elem35;
                _elem35 = g_object_new (TYPE_KEY_SLICE, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem35), protocol, error)) < 0)
                  return 0;
                xfer += ret;
                g_ptr_array_add (*_return, _elem35);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;

            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_get_range_slices (CassandraIf * iface, GPtrArray ** _return, const ColumnParent * column_parent, const SlicePredicate * predicate, const KeyRange * range, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_get_range_slices (iface, column_parent, predicate, range, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_get_range_slices (iface, _return, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_get_indexed_slices (CassandraIf * iface, const ColumnParent * column_parent, const IndexClause * index_clause, const SlicePredicate * column_predicate, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "get_indexed_slices", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "get_indexed_slices_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_parent", T_STRUCT, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_parent), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "index_clause", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (index_clause), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_predicate", T_STRUCT, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_predicate), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_get_indexed_slices (CassandraIf * iface, GPtrArray ** _return, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "get_indexed_slices", 18) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected get_indexed_slices", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              ThriftType element_type;
              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type, &size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              guint32 i;
              for (i = 0; i < size; i++)
              {
                KeySlice * _elem36;
                _elem36 = g_object_new (TYPE_KEY_SLICE, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem36), protocol, error)) < 0)
                  return 0;
                xfer += ret;
                g_ptr_array_add (*_return, _elem36);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;

            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_get_indexed_slices (CassandraIf * iface, GPtrArray ** _return, const ColumnParent * column_parent, const IndexClause * index_clause, const SlicePredicate * column_predicate, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_get_indexed_slices (iface, column_parent, index_clause, column_predicate, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_get_indexed_slices (iface, _return, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_insert (CassandraIf * iface, const GByteArray * key, const ColumnParent * column_parent, const Column * column, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "insert", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "insert_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "key", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) key)->data, ((GByteArray *) key)->len, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_parent", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_parent), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column", T_STRUCT, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_insert (CassandraIf * iface, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "insert", 6) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected insert", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_insert (CassandraIf * iface, const GByteArray * key, const ColumnParent * column_parent, const Column * column, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_insert (iface, key, column_parent, column, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_insert (iface, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_add (CassandraIf * iface, const GByteArray * key, const ColumnParent * column_parent, const CounterColumn * column, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "add", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "add_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "key", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) key)->data, ((GByteArray *) key)->len, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_parent", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_parent), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column", T_STRUCT, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_add (CassandraIf * iface, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "add", 3) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected add", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_add (CassandraIf * iface, const GByteArray * key, const ColumnParent * column_parent, const CounterColumn * column, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_add (iface, key, column_parent, column, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_add (iface, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_remove (CassandraIf * iface, const GByteArray * key, const ColumnPath * column_path, const gint64 timestamp, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "remove", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "remove_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "key", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) key)->data, ((GByteArray *) key)->len, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_path", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (column_path), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "timestamp", T_I64, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i64 (protocol, timestamp, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_remove (CassandraIf * iface, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "remove", 6) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected remove", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_remove (CassandraIf * iface, const GByteArray * key, const ColumnPath * column_path, const gint64 timestamp, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_remove (iface, key, column_path, timestamp, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_remove (iface, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_remove_counter (CassandraIf * iface, const GByteArray * key, const ColumnPath * path, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "remove_counter", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "remove_counter_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "key", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) key)->data, ((GByteArray *) key)->len, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "path", T_STRUCT, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (path), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_remove_counter (CassandraIf * iface, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "remove_counter", 14) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected remove_counter", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_remove_counter (CassandraIf * iface, const GByteArray * key, const ColumnPath * path, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_remove_counter (iface, key, path, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_remove_counter (iface, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_batch_mutate (CassandraIf * iface, const GHashTable * mutation_map, const ConsistencyLevel consistency_level, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "batch_mutate", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "batch_mutate_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "mutation_map", T_MAP, 1, error)) < 0)
      return 0;
    xfer += ret;
    {
      if ((ret = thrift_protocol_write_map_begin (protocol, T_STRING, T_MAP, (gint32) g_hash_table_size ((GHashTable *) mutation_map), error)) < 0)
        return 0;
      xfer += ret;

      GList *key_list = NULL, *iter = NULL;
      GByteArray * key;
      GHashTable * value;
      g_hash_table_foreach ((GHashTable *) mutation_map, thrift_hash_table_get_keys, &key_list);
      GByteArray * keys[g_list_length (key_list)];
      int i=0, key_count = g_list_length (key_list);
      for (iter = g_list_first (key_list); iter; iter = iter->next)
      {
        keys[i++] = (GByteArray *) iter->data;
      }
      g_list_free (key_list);

      for (i = 0; i < key_count; ++i)
      {
        key = keys[i];
        value = (GHashTable *) g_hash_table_lookup (((GHashTable *) mutation_map), (gpointer) key);

        if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *)  key)->data, ((GByteArray *)  key)->len, error)) < 0)
          return 0;
        {
          if ((ret = thrift_protocol_write_map_begin (protocol, T_STRING, T_LIST, (gint32) g_hash_table_size ((GHashTable *)  value), error)) < 0)
            return 0;
          xfer += ret;

          GList *key_list = NULL, *iter = NULL;
          gchar * key;
          GPtrArray * value2; /* Zabbix: renamed from "value" to "value2" so as not to shadow the "value" hash table! */
          g_hash_table_foreach ((GHashTable *)  value, thrift_hash_table_get_keys, &key_list);
          gchar * keys[g_list_length (key_list)];
          int i=0, key_count = g_list_length (key_list);
          for (iter = g_list_first (key_list); iter; iter = iter->next)
          {
            keys[i++] = (gchar *) iter->data;
          }
          g_list_free (key_list);

          for (i = 0; i < key_count; ++i)
          {
            key = keys[i];
            value2 = (GPtrArray *) g_hash_table_lookup (((GHashTable *)  value), (gpointer) key); /* Zabbix */

            if ((ret = thrift_protocol_write_string (protocol,  key, error)) < 0)
              return 0;
            {
              if ((ret = thrift_protocol_write_list_begin (protocol, T_STRUCT, (gint32)  value2->len, error)) < 0) /* Zabbix */
                return 0;
              xfer += ret;
              guint i;
              for (i=0; i< value2->len; i++) /* Zabbix */
              {
                if ((ret = thrift_struct_write (THRIFT_STRUCT (g_ptr_array_index ((GPtrArray *)  value2, i)), protocol, error)) < 0) /* Zabbix */
                  return 0;
                xfer += ret;

              }
              if ((ret = thrift_protocol_write_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          }
          if ((ret = thrift_protocol_write_map_end (protocol, error)) < 0)
            return 0;
          xfer += ret;
        }
      }
      if ((ret = thrift_protocol_write_map_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "consistency_level", T_I32, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) consistency_level, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_batch_mutate (CassandraIf * iface, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "batch_mutate", 12) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected batch_mutate", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_batch_mutate (CassandraIf * iface, const GHashTable * mutation_map, const ConsistencyLevel consistency_level, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, GError ** error)
{
  if (!cassandra_client_send_batch_mutate (iface, mutation_map, consistency_level, error))
    return FALSE;
  if (!cassandra_client_recv_batch_mutate (iface, ire, ue, te, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_truncate (CassandraIf * iface, const gchar * cfname, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "truncate", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "truncate_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "cfname", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_string (protocol, cfname, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_truncate (CassandraIf * iface, InvalidRequestException ** ire, UnavailableException ** ue, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "truncate", 8) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected truncate", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_truncate (CassandraIf * iface, const gchar * cfname, InvalidRequestException ** ire, UnavailableException ** ue, GError ** error)
{
  if (!cassandra_client_send_truncate (iface, cfname, error))
    return FALSE;
  if (!cassandra_client_recv_truncate (iface, ire, ue, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_describe_schema_versions (CassandraIf * iface, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "describe_schema_versions", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "describe_schema_versions_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_describe_schema_versions (CassandraIf * iface, GHashTable ** _return, InvalidRequestException ** ire, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "describe_schema_versions", 24) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected describe_schema_versions", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_MAP)
          {
            {
              guint32 size;
              ThriftType key_type;
              ThriftType value_type;

              /* read the map begin marker */
              if ((ret = thrift_protocol_read_map_begin (protocol, &key_type, &value_type, &size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through each of the map's fields */
              guint32 i;
              for (i = 0; i < size; i++)
              {
                gchar * key37;
                GPtrArray * val38;
                if ((ret = thrift_protocol_read_string (protocol, &key37, error)) < 0)
                  return 0;
                xfer += ret;
                {
                  guint32 size;
                  ThriftType element_type;
                  if ((ret = thrift_protocol_read_list_begin (protocol, &element_type, &size, error)) < 0)
                    return 0;
                  xfer += ret;

                  /* iterate through list elements */
                  guint32 i;
                  for (i = 0; i < size; i++)
                  {
                    gchar * _elem39 = NULL;
                    if ((ret = thrift_protocol_read_string (protocol, &_elem39, error)) < 0)
                      return 0;
                    xfer += ret;
                    g_ptr_array_add (val38, _elem39);
                  }
                  if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                    return 0;
                  xfer += ret;

                }
                g_hash_table_insert ((GHashTable *)*_return, (gpointer) &key37, (gpointer) &val38);
              }

              /* read the map end marker */
              if ((ret = thrift_protocol_read_map_end (protocol, error)) < 0)
                return 0;
              xfer += ret;
            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_describe_schema_versions (CassandraIf * iface, GHashTable ** _return, InvalidRequestException ** ire, GError ** error)
{
  if (!cassandra_client_send_describe_schema_versions (iface, error))
    return FALSE;
  if (!cassandra_client_recv_describe_schema_versions (iface, _return, ire, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_describe_keyspaces (CassandraIf * iface, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "describe_keyspaces", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "describe_keyspaces_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_describe_keyspaces (CassandraIf * iface, GPtrArray ** _return, InvalidRequestException ** ire, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "describe_keyspaces", 18) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected describe_keyspaces", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              ThriftType element_type;
              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type, &size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              guint32 i;
              for (i = 0; i < size; i++)
              {
                KsDef * _elem40;
                _elem40 = g_object_new (TYPE_KS_DEF, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem40), protocol, error)) < 0)
                  return 0;
                xfer += ret;
                g_ptr_array_add (*_return, _elem40);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;

            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_describe_keyspaces (CassandraIf * iface, GPtrArray ** _return, InvalidRequestException ** ire, GError ** error)
{
  if (!cassandra_client_send_describe_keyspaces (iface, error))
    return FALSE;
  if (!cassandra_client_recv_describe_keyspaces (iface, _return, ire, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_describe_cluster_name (CassandraIf * iface, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "describe_cluster_name", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "describe_cluster_name_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_describe_cluster_name (CassandraIf * iface, gchar ** _return, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "describe_cluster_name", 21) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected describe_cluster_name", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_describe_cluster_name (CassandraIf * iface, gchar ** _return, GError ** error)
{
  if (!cassandra_client_send_describe_cluster_name (iface, error))
    return FALSE;
  if (!cassandra_client_recv_describe_cluster_name (iface, _return, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_describe_version (CassandraIf * iface, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "describe_version", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "describe_version_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_describe_version (CassandraIf * iface, gchar ** _return, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "describe_version", 16) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected describe_version", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_describe_version (CassandraIf * iface, gchar ** _return, GError ** error)
{
  if (!cassandra_client_send_describe_version (iface, error))
    return FALSE;
  if (!cassandra_client_recv_describe_version (iface, _return, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_describe_ring (CassandraIf * iface, const gchar * keyspace, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "describe_ring", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "describe_ring_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "keyspace", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_string (protocol, keyspace, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_describe_ring (CassandraIf * iface, GPtrArray ** _return, InvalidRequestException ** ire, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "describe_ring", 13) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected describe_ring", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              ThriftType element_type;
              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type, &size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              guint32 i;
              for (i = 0; i < size; i++)
              {
                TokenRange * _elem41;
                _elem41 = g_object_new (TYPE_TOKEN_RANGE, NULL);
                if ((ret = thrift_struct_read (THRIFT_STRUCT (_elem41), protocol, error)) < 0)
                  return 0;
                xfer += ret;
                g_ptr_array_add (*_return, _elem41);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;

            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_describe_ring (CassandraIf * iface, GPtrArray ** _return, const gchar * keyspace, InvalidRequestException ** ire, GError ** error)
{
  if (!cassandra_client_send_describe_ring (iface, keyspace, error))
    return FALSE;
  if (!cassandra_client_recv_describe_ring (iface, _return, ire, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_describe_partitioner (CassandraIf * iface, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "describe_partitioner", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "describe_partitioner_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_describe_partitioner (CassandraIf * iface, gchar ** _return, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "describe_partitioner", 20) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected describe_partitioner", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_describe_partitioner (CassandraIf * iface, gchar ** _return, GError ** error)
{
  if (!cassandra_client_send_describe_partitioner (iface, error))
    return FALSE;
  if (!cassandra_client_recv_describe_partitioner (iface, _return, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_describe_snitch (CassandraIf * iface, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "describe_snitch", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "describe_snitch_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_describe_snitch (CassandraIf * iface, gchar ** _return, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "describe_snitch", 15) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected describe_snitch", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_describe_snitch (CassandraIf * iface, gchar ** _return, GError ** error)
{
  if (!cassandra_client_send_describe_snitch (iface, error))
    return FALSE;
  if (!cassandra_client_recv_describe_snitch (iface, _return, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_describe_keyspace (CassandraIf * iface, const gchar * keyspace, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "describe_keyspace", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "describe_keyspace_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "keyspace", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_string (protocol, keyspace, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_describe_keyspace (CassandraIf * iface, KsDef ** _return, NotFoundException ** nfe, InvalidRequestException ** ire, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "describe_keyspace", 17) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected describe_keyspace", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRUCT)
          {
            *_return = g_object_new (TYPE_KS_DEF, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*_return), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *nfe = g_object_new (TYPE_NOT_FOUND_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*nfe), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*nfe != NULL)
  {
      g_set_error (error, NOT_FOUND_EXCEPTION_ERROR, NOT_FOUND_EXCEPTION_ERROR_CODE, "NotFoundException");
      return FALSE;
  }
  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_describe_keyspace (CassandraIf * iface, KsDef ** _return, const gchar * keyspace, NotFoundException ** nfe, InvalidRequestException ** ire, GError ** error)
{
  if (!cassandra_client_send_describe_keyspace (iface, keyspace, error))
    return FALSE;
  if (!cassandra_client_recv_describe_keyspace (iface, _return, nfe, ire, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_describe_splits (CassandraIf * iface, const gchar * cfName, const gchar * start_token, const gchar * end_token, const gint32 keys_per_split, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "describe_splits", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "describe_splits_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "cfName", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_string (protocol, cfName, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "start_token", T_STRING, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_string (protocol, start_token, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "end_token", T_STRING, 3, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_string (protocol, end_token, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "keys_per_split", T_I32, 4, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, keys_per_split, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_describe_splits (CassandraIf * iface, GPtrArray ** _return, InvalidRequestException ** ire, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "describe_splits", 15) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected describe_splits", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_LIST)
          {
            {
              guint32 size;
              ThriftType element_type;
              if ((ret = thrift_protocol_read_list_begin (protocol, &element_type, &size, error)) < 0)
                return 0;
              xfer += ret;

              /* iterate through list elements */
              guint32 i;
              for (i = 0; i < size; i++)
              {
                gchar * _elem42 = NULL;
                if ((ret = thrift_protocol_read_string (protocol, &_elem42, error)) < 0)
                  return 0;
                xfer += ret;
                g_ptr_array_add (*_return, _elem42);
              }
              if ((ret = thrift_protocol_read_list_end (protocol, error)) < 0)
                return 0;
              xfer += ret;

            }
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_describe_splits (CassandraIf * iface, GPtrArray ** _return, const gchar * cfName, const gchar * start_token, const gchar * end_token, const gint32 keys_per_split, InvalidRequestException ** ire, GError ** error)
{
  if (!cassandra_client_send_describe_splits (iface, cfName, start_token, end_token, keys_per_split, error))
    return FALSE;
  if (!cassandra_client_recv_describe_splits (iface, _return, ire, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_system_add_column_family (CassandraIf * iface, const CfDef * cf_def, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "system_add_column_family", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "system_add_column_family_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "cf_def", T_STRUCT, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (cf_def), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_system_add_column_family (CassandraIf * iface, gchar ** _return, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "system_add_column_family", 24) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected system_add_column_family", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *sde = g_object_new (TYPE_SCHEMA_DISAGREEMENT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*sde), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*sde != NULL)
  {
      g_set_error (error, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR_CODE, "SchemaDisagreementException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_system_add_column_family (CassandraIf * iface, gchar ** _return, const CfDef * cf_def, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{
  if (!cassandra_client_send_system_add_column_family (iface, cf_def, error))
    return FALSE;
  if (!cassandra_client_recv_system_add_column_family (iface, _return, ire, sde, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_system_drop_column_family (CassandraIf * iface, const gchar * column_family, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "system_drop_column_family", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "system_drop_column_family_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "column_family", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_string (protocol, column_family, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_system_drop_column_family (CassandraIf * iface, gchar ** _return, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "system_drop_column_family", 25) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected system_drop_column_family", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *sde = g_object_new (TYPE_SCHEMA_DISAGREEMENT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*sde), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*sde != NULL)
  {
      g_set_error (error, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR_CODE, "SchemaDisagreementException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_system_drop_column_family (CassandraIf * iface, gchar ** _return, const gchar * column_family, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{
  if (!cassandra_client_send_system_drop_column_family (iface, column_family, error))
    return FALSE;
  if (!cassandra_client_recv_system_drop_column_family (iface, _return, ire, sde, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_system_add_keyspace (CassandraIf * iface, const KsDef * ks_def, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "system_add_keyspace", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "system_add_keyspace_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "ks_def", T_STRUCT, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (ks_def), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_system_add_keyspace (CassandraIf * iface, gchar ** _return, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "system_add_keyspace", 19) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected system_add_keyspace", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *sde = g_object_new (TYPE_SCHEMA_DISAGREEMENT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*sde), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*sde != NULL)
  {
      g_set_error (error, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR_CODE, "SchemaDisagreementException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_system_add_keyspace (CassandraIf * iface, gchar ** _return, const KsDef * ks_def, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{
  if (!cassandra_client_send_system_add_keyspace (iface, ks_def, error))
    return FALSE;
  if (!cassandra_client_recv_system_add_keyspace (iface, _return, ire, sde, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_system_drop_keyspace (CassandraIf * iface, const gchar * keyspace, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "system_drop_keyspace", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "system_drop_keyspace_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "keyspace", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_string (protocol, keyspace, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_system_drop_keyspace (CassandraIf * iface, gchar ** _return, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "system_drop_keyspace", 20) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected system_drop_keyspace", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *sde = g_object_new (TYPE_SCHEMA_DISAGREEMENT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*sde), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*sde != NULL)
  {
      g_set_error (error, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR_CODE, "SchemaDisagreementException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_system_drop_keyspace (CassandraIf * iface, gchar ** _return, const gchar * keyspace, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{
  if (!cassandra_client_send_system_drop_keyspace (iface, keyspace, error))
    return FALSE;
  if (!cassandra_client_recv_system_drop_keyspace (iface, _return, ire, sde, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_system_update_keyspace (CassandraIf * iface, const KsDef * ks_def, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "system_update_keyspace", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "system_update_keyspace_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "ks_def", T_STRUCT, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (ks_def), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_system_update_keyspace (CassandraIf * iface, gchar ** _return, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "system_update_keyspace", 22) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected system_update_keyspace", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *sde = g_object_new (TYPE_SCHEMA_DISAGREEMENT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*sde), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*sde != NULL)
  {
      g_set_error (error, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR_CODE, "SchemaDisagreementException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_system_update_keyspace (CassandraIf * iface, gchar ** _return, const KsDef * ks_def, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{
  if (!cassandra_client_send_system_update_keyspace (iface, ks_def, error))
    return FALSE;
  if (!cassandra_client_recv_system_update_keyspace (iface, _return, ire, sde, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_system_update_column_family (CassandraIf * iface, const CfDef * cf_def, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "system_update_column_family", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "system_update_column_family_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "cf_def", T_STRUCT, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_struct_write (THRIFT_STRUCT (cf_def), protocol, error)) < 0)
      return 0;
    xfer += ret;

    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_system_update_column_family (CassandraIf * iface, gchar ** _return, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "system_update_column_family", 27) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected system_update_column_family", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRING)
          {
            if ((ret = thrift_protocol_read_string (protocol, &*_return, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *sde = g_object_new (TYPE_SCHEMA_DISAGREEMENT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*sde), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*sde != NULL)
  {
      g_set_error (error, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR_CODE, "SchemaDisagreementException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_system_update_column_family (CassandraIf * iface, gchar ** _return, const CfDef * cf_def, InvalidRequestException ** ire, SchemaDisagreementException ** sde, GError ** error)
{
  if (!cassandra_client_send_system_update_column_family (iface, cf_def, error))
    return FALSE;
  if (!cassandra_client_recv_system_update_column_family (iface, _return, ire, sde, error))
    return FALSE;
  return TRUE;
}

gboolean cassandra_client_send_execute_cql_query (CassandraIf * iface, const GByteArray * query, const Compression compression, GError ** error)
{
  gint32 cseqid = 0;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->output_protocol;

  if (thrift_protocol_write_message_begin (protocol, "execute_cql_query", T_CALL, cseqid, error) < 0)
    return FALSE;

  {
    gint32 ret;
    gint32 xfer = 0;

    
    if ((ret = thrift_protocol_write_struct_begin (protocol, "execute_cql_query_args", error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "query", T_STRING, 1, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_binary (protocol, ((GByteArray *) query)->data, ((GByteArray *) query)->len, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_begin (protocol, "compression", T_I32, 2, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_i32 (protocol, (gint32) compression, error)) < 0)
      return 0;
    if ((ret = thrift_protocol_write_field_end (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_field_stop (protocol, error)) < 0)
      return 0;
    xfer += ret;
    if ((ret = thrift_protocol_write_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_write_message_end (protocol, error) < 0)
    return FALSE;
  if (!thrift_transport_flush (protocol->transport, error))
    return FALSE;
  if (!thrift_transport_write_end (protocol->transport, error))
    return FALSE;

  return TRUE;
}

gboolean cassandra_client_recv_execute_cql_query (CassandraIf * iface, CqlResult ** _return, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, SchemaDisagreementException ** sde, GError ** error)
{

  gint32 rseqid;
  gchar * fname;
  ThriftMessageType mtype;
  ThriftProtocol * protocol = CASSANDRA_CLIENT (iface)->input_protocol;

  if (thrift_protocol_read_message_begin (protocol, &fname, &mtype, &rseqid, error) < 0)
  {
    if (fname) g_free (fname);
    return FALSE;
  }

  if (mtype == T_EXCEPTION) {
    if (fname) g_free (fname);
    ThriftApplicationException *xception = g_object_new (THRIFT_TYPE_APPLICATION_EXCEPTION, NULL);
    thrift_struct_read (THRIFT_STRUCT (xception), protocol, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, xception->type, "application error: %s", xception->message);
    g_object_unref (xception);
    return FALSE;
  } else if (mtype != T_REPLY) {
    if (fname) g_free (fname);
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, NULL);
    thrift_transport_read_end (protocol->transport, NULL);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_INVALID_MESSAGE_TYPE, "invalid message type %d, expected T_REPLY", mtype);
    return FALSE;
  } else if (strncmp (fname, "execute_cql_query", 17) != 0) {
    thrift_protocol_skip (protocol, T_STRUCT, NULL);
    thrift_protocol_read_message_end (protocol, error);
    thrift_transport_read_end (protocol->transport, error);
    g_set_error (error, THRIFT_APPLICATION_EXCEPTION_ERROR, THRIFT_APPLICATION_EXCEPTION_ERROR_WRONG_METHOD_NAME, "wrong method name %s, expected execute_cql_query", fname);
    if (fname) g_free (fname);
    return FALSE;
  }
  if (fname) g_free (fname);

  {
    gint32 ret;
    gint32 xfer = 0;
    gchar *name;
    ThriftType ftype;
    gint16 fid;
    guint32 len = 0;
    gpointer data = NULL;
    

    /* satisfy -Wall in case these aren't used */
    THRIFT_UNUSED_VAR (len);
    THRIFT_UNUSED_VAR (data);

    /* read the struct begin marker */
    if ((ret = thrift_protocol_read_struct_begin (protocol, &name, error)) < 0)
    {
      if (name) g_free (name);
      return 0;
    }
    xfer += ret;
    if (name) g_free (name);

    /* read the struct fields */
    while (1)
    {
      /* read the beginning of a field */
      if ((ret = thrift_protocol_read_field_begin (protocol, &name, &ftype, &fid, error)) < 0)
      {
        if (name) g_free (name);
        return 0;
      }
      xfer += ret;
      if (name) g_free (name);

      /* break if we get a STOP field */
      if (ftype == T_STOP)
      {
        break;
      }

      switch (fid)
      {
        case 0:
          if (ftype == T_STRUCT)
          {
            *_return = g_object_new (TYPE_CQL_RESULT, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*_return), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 1:
          if (ftype == T_STRUCT)
          {
            *ire = g_object_new (TYPE_INVALID_REQUEST_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ire), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 2:
          if (ftype == T_STRUCT)
          {
            *ue = g_object_new (TYPE_UNAVAILABLE_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*ue), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 3:
          if (ftype == T_STRUCT)
          {
            *te = g_object_new (TYPE_TIMED_OUT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*te), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        case 4:
          if (ftype == T_STRUCT)
          {
            *sde = g_object_new (TYPE_SCHEMA_DISAGREEMENT_EXCEPTION, NULL);
            if ((ret = thrift_struct_read (THRIFT_STRUCT (*sde), protocol, error)) < 0)
              return 0;
            xfer += ret;
          } else {
            if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
              return 0;
            xfer += ret;
          }
          break;
        default:
          if ((ret = thrift_protocol_skip (protocol, ftype, error)) < 0)
            return 0;
          xfer += ret;
          break;
      }
      if ((ret = thrift_protocol_read_field_end (protocol, error)) < 0)
        return 0;
      xfer += ret;
    }

    if ((ret = thrift_protocol_read_struct_end (protocol, error)) < 0)
      return 0;
    xfer += ret;

  }

  if (thrift_protocol_read_message_end (protocol, error) < 0)
    return FALSE;

  if (!thrift_transport_read_end (protocol->transport, error))
    return FALSE;

  if (*ire != NULL)
  {
      g_set_error (error, INVALID_REQUEST_EXCEPTION_ERROR, INVALID_REQUEST_EXCEPTION_ERROR_CODE, "InvalidRequestException");
      return FALSE;
  }
  if (*ue != NULL)
  {
      g_set_error (error, UNAVAILABLE_EXCEPTION_ERROR, UNAVAILABLE_EXCEPTION_ERROR_CODE, "UnavailableException");
      return FALSE;
  }
  if (*te != NULL)
  {
      g_set_error (error, TIMED_OUT_EXCEPTION_ERROR, TIMED_OUT_EXCEPTION_ERROR_CODE, "TimedOutException");
      return FALSE;
  }
  if (*sde != NULL)
  {
      g_set_error (error, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR, SCHEMA_DISAGREEMENT_EXCEPTION_ERROR_CODE, "SchemaDisagreementException");
      return FALSE;
  }
  return TRUE;
}

gboolean cassandra_client_execute_cql_query (CassandraIf * iface, CqlResult ** _return, const GByteArray * query, const Compression compression, InvalidRequestException ** ire, UnavailableException ** ue, TimedOutException ** te, SchemaDisagreementException ** sde, GError ** error)
{
  if (!cassandra_client_send_execute_cql_query (iface, query, compression, error))
    return FALSE;
  if (!cassandra_client_recv_execute_cql_query (iface, _return, ire, ue, te, sde, error))
    return FALSE;
  return TRUE;
}

static void
cassandra_if_interface_init (CassandraIfInterface *iface)
{
  iface->login = cassandra_client_login;
  iface->set_keyspace = cassandra_client_set_keyspace;
  iface->get = cassandra_client_get;
  iface->get_slice = cassandra_client_get_slice;
  iface->get_count = cassandra_client_get_count;
  iface->multiget_slice = cassandra_client_multiget_slice;
  iface->multiget_count = cassandra_client_multiget_count;
  iface->get_range_slices = cassandra_client_get_range_slices;
  iface->get_indexed_slices = cassandra_client_get_indexed_slices;
  iface->insert = cassandra_client_insert;
  iface->add = cassandra_client_add;
  iface->remove = cassandra_client_remove;
  iface->remove_counter = cassandra_client_remove_counter;
  iface->batch_mutate = cassandra_client_batch_mutate;
  iface->truncate = cassandra_client_truncate;
  iface->describe_schema_versions = cassandra_client_describe_schema_versions;
  iface->describe_keyspaces = cassandra_client_describe_keyspaces;
  iface->describe_cluster_name = cassandra_client_describe_cluster_name;
  iface->describe_version = cassandra_client_describe_version;
  iface->describe_ring = cassandra_client_describe_ring;
  iface->describe_partitioner = cassandra_client_describe_partitioner;
  iface->describe_snitch = cassandra_client_describe_snitch;
  iface->describe_keyspace = cassandra_client_describe_keyspace;
  iface->describe_splits = cassandra_client_describe_splits;
  iface->system_add_column_family = cassandra_client_system_add_column_family;
  iface->system_drop_column_family = cassandra_client_system_drop_column_family;
  iface->system_add_keyspace = cassandra_client_system_add_keyspace;
  iface->system_drop_keyspace = cassandra_client_system_drop_keyspace;
  iface->system_update_keyspace = cassandra_client_system_update_keyspace;
  iface->system_update_column_family = cassandra_client_system_update_column_family;
  iface->execute_cql_query = cassandra_client_execute_cql_query;
}

static void
cassandra_client_init (CassandraClient *client)
{
  client->input_protocol = NULL;
  client->output_protocol = NULL;
}

static void
cassandra_client_class_init (CassandraClientClass *cls)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (cls);
  GParamSpec *param_spec;

  gobject_class->set_property = cassandra_client_set_property;
  gobject_class->get_property = cassandra_client_get_property;

  param_spec = g_param_spec_object ("input_protocol",
                                    "input protocol (construct)",
                                    "Set the client input protocol",
                                    THRIFT_TYPE_PROTOCOL,
                                    G_PARAM_READWRITE);
  g_object_class_install_property (gobject_class,
                                   PROP_CASSANDRA_CLIENT_INPUT_PROTOCOL, param_spec);

  param_spec = g_param_spec_object ("output_protocol",
                                    "output protocol (construct)",
                                    "Set the client output protocol",
                                    THRIFT_TYPE_PROTOCOL,
                                    G_PARAM_READWRITE);
  g_object_class_install_property (gobject_class,
                                   PROP_CASSANDRA_CLIENT_OUTPUT_PROTOCOL, param_spec);
}

