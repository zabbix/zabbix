<?xml version="1.0"?>
<ruleset name="DuoLinting">
    <description>This standard changes the line length</description>

    <!-- Exclude line lengths that are too long -->
    <rule ref="PSR2">
        <exclude name="Generic.Files.LineLength" />
    </rule>
</ruleset>

