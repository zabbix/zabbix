steps:

  # test that valid API calls are executed correctly
  # the exact API call performed here is unimportant, so long as it can be executed successfully
  # and validated
  step1:
    request:
      id: 1
      method: user.login
      params:
        user: Admin
        password: zabbix

    assertResponse:
      jsonrpc: "2.0"
      result:
        _assert: { string: { regex: "/^[a-zA-Z0-9]{32}$/" } }
      id: 1
