steps:
    step1:
        request:
            id: 1004
            method: validate1
            params: []
        expect: response
        response:
            _assert:
                _keys:
                    _each:
                        _assert: int
                _each:
                    id:
                        _assert: notEmpty|int
                    hostname:
                        _assert: notEmpty|string|length(5,10)
