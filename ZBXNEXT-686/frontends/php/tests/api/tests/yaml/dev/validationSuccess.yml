steps:
    step1:
        request:
            id: 1002
            method: validate1
            params: []
        expect: response
        response:
        # it should contain array if [id: decimal, hostname: string, templates; <array of string>]
            _assert:
                _keys:
                    _each:
                        _assert: int
                _each:
                    id:
                        _assert: notEmpty|int
                    hostname:
                        _assert: notEmpty|string|length(5,10)
                    templates:
                        _each:
                            _assert: string|length(0,12)
