{
	"config": {
		"__comment__": "This prevents composer to generate ComposerAutoloaderInitSOMEMD5HASHHERE classes on each atuloader dump",
		"autoloader-suffix": "Zabbix"
	},
	"__comment__": "We bind to concrete versions to avoid updated and prefer zip/tarballs",
	"preferred-install": "dist",
	"require": {
		"symfony/yaml": "2.5",
		"phpunit/phpunit": "4.1.4",
		"respect/validation": "0.6.1"
	},
	"__comment__": "Lines below will be needed after vendors are merged to zabbix codebase",
	"scripts": {
		"post-update-cmd": ["echo [WARNING] Delete all .git dirs", "rm -rf vendor/**/**/.git"],
		"post-install-cmd": ["echo [WARNING] Delete all .git dirs", "rm -rf vendor/**/**/.git"]
	},
	"autoload": {
		"psr-0": {
			"Zabbix\\": "src/",
			"Zabbix\\Tests\\": "tests/",
            "Respect\\": "src/"
		},
		"__comment__": "Add a list of directories here - all files from them will be autoloaded",
		"classmap": [
		],
		"__comment__": "Put paths to all files that can not be autoloaded yet needs to be included",
		"files": [
		]
	}
}
