steps:
    step1:
        request:
            id: 1001
            method: validate1
            params: []
        expect: response
        response:
        # it should contain array if [id: decimal, hostname: string, templates; <array of string>]
            _assert:
                _keys:
                    _assert: length(3)
                    _each:
                        _assert: int
