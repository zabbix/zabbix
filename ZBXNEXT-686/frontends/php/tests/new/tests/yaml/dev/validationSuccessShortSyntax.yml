steps:
    step1:
        request:
            id: 1002
            method: validate1
            params: []
        expect: response
        response:
        # it should contain array if [id: decimal, hostname: string, templates; <array of string>]
            _assert:
                _keys:
                    _assert: length(2)
                    _each: int
                _each:
                    _assert: length(3)
                    id: notEmpty|int
                    hostname: notEmpty|string|length(5,10)
                    templates:
                        _each: string|length(0,12)
